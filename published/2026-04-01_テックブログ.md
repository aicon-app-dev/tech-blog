# AIエージェント × GitHub Actions で自律開発環境を2ヶ月で構築した話

## 1. 我々について

### 1-1. 会社・プロダクトの概要

<!-- TODO: サービス名・プロダクト概要を記載 -->

我々は〇〇というプロダクトを開発しているスタートアップだ。プロダクトはモバイルアプリ（React Native / Expo）とオペレーター向け管理画面、およびそれらを支えるバックエンドで構成されている。モノレポ構成で、カスタマー向けモバイル・オペレーターフロントエンド・バックエンドが同一リポジトリに同居している。

### 1-2. チーム構成

人間のエンジニアは数名。それに対して、GitHub Actions 上で稼働する AIエージェントが現時点で **約30体以上** 存在する。人間がIssueを立てて寝れば、朝にはマージレディのPRが上がっている——というのが、今の我々の開発スタイルだ。

<!-- 
TODO【スクショ】: GitHub Projectsのボードを俯瞰したスクリーンショット
  → 左から右にカードが並んでいる全景。「人間数名 + AIエージェント30体以上」のスケール感が伝わる絵面
  → 機密情報はモザイク処理
-->

<!--
TODO【ポンチ絵】: チーム構成図
  → 人間アイコン数名 + AIエージェントアイコン30体以上が並ぶ簡易図
  → 「人間 → Issue投入」「AIエージェント → 設計/実装/レビュー/デプロイ」の役割分担が一目でわかるもの
-->

### 1-3. この記事で伝えたいこと

本記事はエンジニア向けに、我々がこの2ヶ月で構築した自律開発パイプラインの **事実** を淡々と書く。何を使い、何を設定し、何ができるようになり、何がまだできていないか。哲学やポエムは最小限にとどめる。

この記事の設定やワークフローをそのまま持ち帰って、自分たちの環境に適用できることを目指している。

---

## 2. エージェント化以前の状況

### 2-1. 従来の開発フロー・課題

2ヶ月前の開発フローは、一般的なスタートアップのそれだった。

- エンジニアがローカルで開発し、PRを出し、レビューを受けてマージする
- 「あれ今どうなってる？」「これ誰がやってる？」という確認がSlackで飛び交う
- CIは最低限のリント・テストのみ。しかもバックエンドなど一部プロジェクトにしか入っていなかった
- 要件の認識合わせ、設計の確認、実装の分担——すべてが人間同士の口頭コミュニケーションに依存していた

チームが小さいうちはこれで回っていたが、やりたいことに対して圧倒的に手が足りていなかった。

<!--
TODO【スクショ】: 2ヶ月前のSlackやGitHubの様子（あれば）
  → 「これどうなってる？」系のやり取りが飛び交っているSlackスレッド
  → or PRが数日放置されているGitHubの画面
  → ビフォー感が伝わる素材。なければ省略可
-->

<!--
TODO【数字】: 以前の開発ペースの定量データ（あれば）
  → 月間マージPR数、平均PR滞留時間、Issue消化数 etc.
  → 4章で出す「after」の数字と対比できると説得力が出る
-->

### 2-2. なぜ自律開発に踏み切ったか

理由はシンプルで、 **少人数で大量のアウトプットを出す** ためだ。AIエージェントを「もう一人のエンジニア」として開発プロセスに組み込めば、人間はより上流の意思決定に集中できる。ただし「AIに丸投げ」では品質が担保できない。人間が安心して任せられる仕組みをどう作るかが、この2ヶ月の本題だった。

---

## 3. 技術スタック

### 3-1. 全体構成

自律開発パイプラインを支える主要な技術スタックは以下の通り。

<!--
TODO【ポンチ絵】: 全体アーキテクチャ図
  → 中央に GitHub Actions、そこから Claude API / Terraform / Expo / Sentry 等が放射状につながる構成図
  → 「Issue起票 → ラベル付与 → Actions発火 → Claude API呼出 → PR作成 → CI → マージ → Terraformデプロイ」の全体フローが1枚で見渡せるもの
  → この図1枚で記事の全容が掴めるのが理想
-->

| 技術 | 役割 |
|---|---|
| **GitHub Actions** | 全自動化の基盤。エージェントの起動・CI/CD・Issue管理すべてここで動く |
| **Claude API** | 各AIエージェントの頭脳。APIキー経由で呼び出し |
| **Claude Code** | ローカル〜CI上でのコード生成・修正に使用 |
| **Bug Bot** | 自動バグ検出 |
| **Terraform** | プレビュー環境の構築・破棄をコードで管理 |
| **Expo** | React Nativeモバイルアプリのビルド・配信 |
| **Left Hook** | コミット前のリント強制実行（pre-commitフック） |
| **Sentry** | フロントエンドのエラー監視（本格活用はこれから） |

### 3-2. なぜこの組み合わせか

設計の原則は一つ。 **開発のすべてをGitHub Actions上で完結させる** こと。

ローカル開発に依存すると、AIエージェントの動作が見えなくなる。GitHub上でIssue → PR → マージまですべてが可視化されていれば、人間のエンジニアが複数人いても「今誰が（どのエージェントが）何をしているか」が一目瞭然になる。Left HookでローカルのコミットにもCIと同じリントを走らせ、二重チェックしている。

<!--
TODO【スクショ】: GitHub Actionsの実行画面
  → 複数のワークフローが同時に走っている様子（Actions一覧画面）
  → 「こんなに同時に動いてるのか」が伝わるスケール感
-->

<!--
TODO【コード片】: Left Hookの設定ファイル（lefthook.yml）の抜粋
  → pre-commitでどのリンターを走らせているか、3〜10行程度
  → 「CIと同じリントがローカルでも走る」の具体がわかるもの
-->

---

## 4. この2ヶ月でやった取り組み

### 4-1. 開発プロセスの分解

最も重要な意思決定は、 **開発プロセスを細かく分解したこと** だ。

我々は以下のステップに分解している。

```
要求定義 → 要件定義 → 基本設計 → 詳細設計 → 実装 → 設計分割 → サブタスク化 → featureブランチ → devマージ
```

<!--
TODO【ポンチ絵】: 開発プロセスのステップ図
  → 上記フローを矢印でつないだ横長の図
  → 各ステップの下に「担当: AIエージェント名」or「担当: 人間」を表記
  → どこが人間のハンドオフポイントかが一目でわかる
-->

<!--
TODO【具体例】: 実際のIssue 1件を例に、このプロセスがどう流れたかの実例
  → 例: 「〇〇機能の追加」という要求が立ってから、devマージまでの時系列
  → 各ステップで何が生成されたか（要件ドキュメント、設計ドキュメント、サブIssue、PR等）
  → スクショ or Issueのリンク or テキスト再現
  → 「夜21時にIssue立てて、翌朝9時にマージレディだった」のような時間軸があるとインパクト大
-->

従来は「Issueを立てて、誰かが実装してPR出す」という粒度だった。これをここまで分解した理由は、 **AIエージェントが一つひとつのステップを確実にこなせるようにするため** だ。一気に全部やらせると品質がブレる。ステップを分ければ、各ステップでの検証が可能になる。

これが我々の自律開発の起点であり、正直これが全てと言ってもいい。

### 4-2. GitHub Projects + Issue駆動の自動化

分解したプロセスを運用に乗せるために、GitHub Projectsを導入した。

**やったこと：**

- Issueに対してPRが自動で紐付く仕組みを構築
- 1つのIssue（要求）から、設計・実装などのサブIssueが自動生成される
- 各Issueのステータスが、プロセスの進行に応じて自動的に左から右へ遷移する
- すべてのコンテキスト（要件、設計、議論、PR）が1つのIssueツリーに集約される

<!--
TODO【スクショ3連】: GitHub Projectsボードのビフォーアフター
  1. 要求Issueが立った直後のボード（左端にカードが1枚ある状態）
  2. サブIssueが自動生成されて各列にカードが配置された状態
  3. 全ステップが完了してマージレディになった状態
  → この3枚で「左から右に自動で流れる」が伝わる
-->

<!--
TODO【スクショ】: 1つのIssueのツリー構造
  → 親Issue（要求）の下にサブIssue（設計、実装×N）がぶら下がり、各サブIssueにPRが紐付いている状態
  → 「1つの要求に対してこれだけの成果物が自動で紐付く」が伝わるもの
-->

<!--
TODO【コード片】: サブIssue自動生成のGitHub Actionsワークフロー抜粋
  → ラベルorイベントをトリガーにIssueを自動作成している箇所、10〜20行程度
  → 「こんな数行でできるのか」感が出ると読者のハードルが下がる
-->

<!--
TODO【コード片】: ステータス自動遷移のActionsワークフロー抜粋
  → PRがマージされた→Projectsのカードを次の列に移動、のような処理
-->

**何が変わったか：**

これが回るようになって、人間同士のコミュニケーションコストが劇的に減った。「あれどうなってる？」がなくなった。Projectボードを見れば、全タスクの進捗が一目瞭然。だから各自が自分の作業に安心して没頭できるようになった。

代表（筆者の上長）も横からフィードバックを入れやすくなったし、「これやっといて」で本当に完了するようになった。AIが自律的に動いて途中で詰まっても、Issueにすべてのコンテキストが載っているから人間が救済しやすい。

正直、この仕組みの構築に最初の2週間を費やした。ここが一番苦戦した。だが、 **ここが回り始めてから、開発速度が一気に跳ね上がった。** 我々にとっては最大の転換点だった。

<!--
TODO【数字】: 導入前後の定量比較（あれば）
  → 週間マージPR数、Issue消化速度、Slackでの「これどうなった？」系メッセージ数の変化 etc.
  → 定量が無理なら「体感でN倍」でもOK
-->

<!--
TODO【スクショ】: 実際に「寝てたらマージレディになっていた」PRの例
  → Issue作成時刻とPR完了時刻が見えるスクショ
  → 深夜〜早朝にエージェントがコミットしている時系列が見えるとインパクト大
-->

### 4-3. ラベル駆動の自律エージェント

GitHub Projects上でプロセスが可視化されたあと、次にやったのが **ラベルによるエージェント自動起動** だ。これが我々のパイプラインの心臓部にあたる。

**仕組み：**

1. Issueやプロジェクトの状態に応じて、適切なラベルが自動付与される
2. ラベルをトリガーにGitHub Actionsが発火し、対応するClaude APIエージェントが起動する
3. エージェントが作業を完了すると、次のステップに対応するラベルを自動で付与する
4. 次のエージェントが起動する

つまり、 **人間がラベルを1つ付ければ（あるいはIssueを立てれば）、あとはエージェント同士がバトンを渡し合って、最終的にマージレディのPRが上がってくる。**

一定以上の判断が必要なポイントでは、人間にレビュー依頼が飛ぶ。完全放任ではなく、人間がハンドオフポイントで介入できる設計にしている。

<!--
TODO【ポンチ絵】: ラベル駆動フローの全体図
  → ラベルA付与 → エージェント①起動 → 作業完了 → ラベルB自動付与 → エージェント②起動 → ... → マージレディ
  → 途中に「人間レビューポイント」が挟まる箇所を明示
  → 色分けで「自動」「人間介入」を区別
-->

<!--
TODO【具体例パターン1】: 新機能追加の典型フロー
  → 実際のラベル名を列挙: 例) `agent:requirements` → `agent:design` → `agent:implement` → `review:human` → `agent:test` → `ready:merge`
  → 各ラベルで何が起きるか1行ずつ
-->

<!--
TODO【具体例パターン2】: バグ修正の典型フロー
  → バグ報告Issue → ラベル付与 → 原因特定エージェント → 修正エージェント → CIパス → マージ
  → 新機能とはフローが違うことを示す
-->

<!--
TODO【スクショ】: Issueに自動でラベルが貼られていく様子
  → GitHubのIssueタイムライン上で、botがラベルを付与→外す→次のラベルを付与 の履歴が見えるスクショ
  → 「エージェントがバトンを渡し合っている」のが視覚的にわかる
-->

<!--
TODO【コード片】: ラベルをトリガーにエージェントを起動するActionsワークフロー
  → on: issues: labeled → if: label == 'agent:implement' → Claude API呼び出し の骨格
  → 10〜20行程度。読者が「これコピペして動かせそう」と思えるレベル
-->

<!--
TODO【コード片】: エージェントが完了後に次のラベルを付与する処理
  → 作業完了 → 現在のラベル剥がす → 次ステップのラベル付与 の部分
  → 「バトンリレー」の実装がこれだけのコードで実現できるのか、という驚き
-->

### 4-4. AIエージェント一覧と各役割

現在稼働しているAIエージェントの一覧を示す。各エージェントにはそれぞれ固有のシステムプロンプト（人格・スキル定義）が設定されている。

| # | ファイル名 | 役割 | 使い方のコツ |
|---|-----------|------|------------|
| 1 | `claude-requirements.yml` | 要求Issueから[要件定義]サブイシューを作成し、質問→回答の壁打ちで要件を詰める。「要件確定」で[詳細設計]サブイシューを自動作成 | Issueのbodyに背景・ゴール・制約を詳しく書くほど壁打ちの質が上がる。`AIが自動的に回答`ラベルと併用すると完全自動化。「要件確定」は正確なキーワード一致が必要 |
| 2 | `claude-auto-answer.yml` | 要件定義・設計フェーズのClaude質問にAIが自動回答。「基本設計確定」「詳細設計確定」も自動判断 | 自動回答できるのはコードパターン・慣習・CLAUDE.md記載事項のみ。ビジネス判断はエスカレーションされるので人間が回答する。壁打ちが止まったら人間がコメントすれば再開 |
| 3 | `claude-detailed-design.yml` | 基本設計→詳細設計の2フェーズを担当。Entスキーマ・GraphQL定義・テスト戦略・PR分割計画(Section 14)を策定し、設計PRを作成 | **Section 14（PR分割計画）が最重要** — タスク分割エージェントの入力になる。壁打ち上限15ラウンドで停止するので、AI自動回答でテンポよく進めるのが効率的 |
| 4 | `claude-task-split.yml` | 設計PRマージ後、Section 14を読み取り[実装]サブイシュー群を作成。依存関係を`blocked_by`で設定 | 設計書のSection 14が曖昧だと不適切な分割になる。手動dispatchでPR番号指定の再実行可能。依存関係が間違うとタスクが永遠に開始されないので注意 |
| 5 | `claude-implement-from-issue.yml` | Issue本文の指示に従いブランチ作成→コード実装→テスト→PR作成を全自動実行。依存Issueがある場合はブロック解除後に着手 | Issue本文に実装指示を具体的に書くほど品質が上がる（ファイルパス・関数名・テストケース等）。`make ent`/`make ggen`等のコード生成も自動実行。タイムアウト60分 |
| 6 | `auto-label-on-unblock.yml` | ブロッカーIssueクローズ時、後続Issueに`IssueからClaudeが自動実装`ラベルを付与して実装を再起動 | 手動でIssueをクローズしても後続が自動起動する。ブロッカーが複数ある場合は全クローズまで起動しない |
| 7 | `create-feature-pr.yml` | 全[実装]サブイシュー完了時にfeat→developのPRを自動作成。Projectステータスを「人間レビュー待ち」に更新 | Feature PRは人間レビュー必須。部分完了では作成されない。develop向けPR作成時に影響分析が自動実行される |
| 8 | `claude-full-auto-pipeline.yml` | Bugbot修正→CI修正→Claudeレビュー→レビュー修正→マージをステートマシンで全自動実行 | `（自動マージ）`付きラベルなら自動マージ、なしなら人間レビュー待ち。上限: Bugbot10回/CI3回/レビュー5サイクル。上限到達で`人間の対応が必要`ラベルが付く |
| 9 | `claude-auto-review-pipeline.yml` | CI→レビュー→修正→マージを自動化。フルオートとの違いはBugbotフェーズがない点 | Bugbot不要なPR（小規模修正・ドキュメント変更等）に適している。レビュー上限10サイクル/CI修正上限3回 |
| 10 | `claude-auto-review.yml` | PRのコードレビューを4カテゴリ（アーキテクチャ/リスク/保守性/設計適合性）で実施。修正は行わない | `Claude自動パイプライン`ラベル付きPRは除外。同一commit SHAへの重複レビューはスキップ。設計ドキュメントがあれば設計適合性チェックも実行 |
| 11 | `claude-auto-review-fix.yml` | Claudeレビューの指摘を自動修正。Critical/High→無条件修正、Medium→ルール確認後修正、Low→スキップ | スコープゲートあり（PR変更ファイルのみ修正可能）。上限5サイクル。フルオートパイプラインの一部として組み込まれることが多い |
| 12 | `claude-bugbot.yml` | cursor[bot]のレビュー指摘をFix/False positive/Won't fix/Questionの4種に仕分けて対応 | False positive判定が重要（Bugbotは誤検知も多い）。人間が質問に回答後に再ラベル付与で再処理可能。上限6回 |
| 13 | `claude-fix-ci.yml` | CI失敗ログを分析し、Lint→型→テスト→コード生成→一時的障害に応じて自動修正 | 一時的障害はファイル編集せず`gh run rerun`のみ。修正後はローカル検証してからpush。上限3回 |
| 14 | `claude-auto-merge.yml` | 全CI通過確認後にPRを自動マージ。フィーチャーブランチマージ時はIssueを明示的にクローズ | GitHubの`closes #`はデフォルトブランチマージ時のみ動作するため、フィーチャーブランチ向けでは明示クローズが必要 |
| 15 | `claude-impact-analysis.yml` | PR変更内容を分析し、影響を受ける機能・テスト項目をPR descriptionに自動追記 | develop向けPR作成時に自動実行されるので手動ラベル不要。既存のdescriptionは保持される |
| 16 | `claude-fix-conflict.yml` | マージコンフリクトを自動解消 | 両方の変更を理解した上で適切にマージする（片方を一方的に捨てない）。`merge-conflict-rule.mdc`に準拠 |
| 17 | `claude-bug-investigation.yml` | バグIssueのコードベースを調査し原因特定。修正は行わず原因分析と修正方針の提示に特化 | 2フェーズ構成: `ongoing`（初期調査）→`confirmed`（結果まとめ）。Browser MCPで画面操作再現も可能。完了後に`IssueからClaudeが自動実装`ラベルで自動修正に接続可能 |
| 18 | `pipeline-stuck-monitor.yml` | 5分間隔でパイプライン停滞を検知。壁打ち停止/PR停止/実装遷移停止/feat PR未作成が対象。自動回復を最大2回試行 | 回復アクションはラベル再付与やワークフロー再実行等の安全な操作のみ。`人間の対応が必要`ラベル付きはスキップ。失敗時はIssue起票+Slack通知でエスカレーション |
| 19 | `sync-develop-to-feature.yml` | `.github/workflows/`等のdevelop push時に全オープンPRへ自動sync。`ブランチ最新化`ラベルでfeatureブランチにdevelopをマージ | 壊れたワークフローをdevelopにマージすると全PRに影響するので注意。`ブランチ最新化`ラベルは手動付与・自動除去 |
| 20 | `update-workflow-readme.yml` | `claude*.yml`変更時に`.github/README.md`を自動更新しPR作成 | ワークフロー追加・変更時にドキュメントが自動追従する仕組み |
| 21 | `detect-unused-code.yml` | 毎週月曜にcustomer-mobileの不要コードを検知して報告 | v1→v2移行に伴いv1側で使われなくなったコードの検出に有用。週次スケジュール実行 |

エージェントがうまく動作する鍵は、 **各エージェントに正しいスキル（システムプロンプト）を与えること** に尽きる。プロンプトの精度がそのまま出力の精度になる。

<!--
TODO【コード片】: 代表的なエージェント1体のシステムプロンプト抜粋
  → 実装エージェント or レビューエージェントなど、わかりやすいものを選定
  → 「こういう観点を与えている」がわかる10〜30行程度
  → 全文公開が難しければ、構造（セクション見出し）だけでもOK
-->

<!--
TODO【コード片】: Claude API呼び出し部分のコード抜粋
  → Actions内でClaude APIを呼び出しているスクリプトの骨格
  → system prompt設定 → Issue本文をuser messageに渡す → レスポンスでPR作成 の流れ
  → 「こうやってAPIを叩いてるのか」が伝わるもの
-->

<!--
TODO【スクショ】: エージェントが実際に書いたPRのdiff画面
  → AIが自動生成したコードのdiffが見える状態
  → 「このクオリティが自動で出るのか」が伝わるもの
-->

### 4-5. CI/CDの拡充

自律開発でAIが大量のコード変更を生成するようになると、CIの重要度が跳ね上がる。人間がすべてのPRを精読するのは物理的に不可能だから、機械的に品質を担保する仕組みが必要になる。

**この2ヶ月で追加・拡張したCIチェック：**

**DBスキーマの破壊的変更チェック**
AIがDBマイグレーションを生成した際に、既存データとの互換性を自動検証する。これがないとAIが意図せず破壊的変更を入れてしまうリスクがあり、安心してリリースできなかった。このチェックが入ったことで、物量を増やしても破壊的変更が混入しないことが担保され、ガンガン新機能を作れるようになった。

<!--
TODO【スクショ】: 破壊的変更チェックが実際にCIで引っかかった例
  → PRのChecksタブで「破壊的変更が検出されました」的なエラーが出ている画面
  → AIが意図せず入れた破壊的変更をCIが止めた、という実例が伝わる
-->

<!--
TODO【コード片】: 破壊的変更チェックのActionsワークフロー or スクリプト抜粋
  → DBスキーマの差分を検出→互換性チェック の核心部分
  → 5〜15行程度
-->

**リント・テストのモノレポ全体展開**
以前はバックエンドにしかリント・テストのCIが入っていなかった。これをモノレポ内の全プロジェクト（カスタマーモバイル、オペレーターフロントエンド含む）に展開した。

<!--
TODO【スクショ】: PRのChecksタブに大量のCIチェックが並んでいる画面
  → 「backend-lint ✅ / backend-test ✅ / mobile-lint ✅ / operator-lint ✅ / schema-check ✅ / ...」
  → 全プロジェクトにCIが入っている網羅感が伝わるもの
-->

<!--
TODO【数字】: CIチェック数のビフォーアフター
  → 以前: N個 → 現在: M個（具体的な数字）
-->

**ドキュメント同期チェック**
コード変更に対してドキュメントが追従しているかを自動検証する。

**テストの着手**
単体テストの記述を開始している。正直に言えばまだまだ不十分な段階で、カバレッジとしては発展途上。ただし、テストが書ける基盤は整った。

Left Hookによるpre-commitフックと合わせて、ローカルでもCI上でも同じリントが走る二重チェック体制になっている。

### 4-6. プレビュー環境

PRごとにプレビュー環境が自動で立ち上がる仕組みをTerraformで構築した。

**仕組み：**

- PRが作成されると、GitHub Actionsが発火しTerraformでプレビュー環境を自動構築
- PRがマージまたはクローズされると、同じくTerraformで環境を自動破棄
- QRコードが生成され、モバイルアプリの動作確認がその場で可能

<!-- TODO: プレビュー環境のスクショ（QRコードにはモザイク）を挿入 -->

<!-- TODO: Terraformの構築・破棄の該当コード片を挿入（数行〜十数行程度） -->

正直、プレビュー環境の自動構築は「大企業がやることで、うちの規模では無理では」と思っていた。手を動かすまでその不安は拭えなかった。だが実際にTerraformで書いてみると、思っていたよりもシンプルに実現できた。

<!--
TODO【スクショ4連】: プレビュー環境のライフサイクル
  1. PRが作成された直後のGitHub画面（Actionsがトリガーされている）
  2. Actions実行中の画面（Terraform applyが走っている）
  3. PRのコメントにプレビューURLとQRコードが自動投稿された画面（QRはモザイク）
  4. スマホでQRを読み取ってプレビュー環境にアクセスしている画面（実機写真 or スクショ）
  → この4枚で「PRを出すだけでスマホで確認できる」の体験が絵で伝わる
-->

<!--
TODO【コード片】: Terraform構築の核心部分
  → PR作成時にプレビュー環境を立ち上げるterraformリソース定義
  → 「こんな数行で環境が立つのか」という驚きが伝わる分量（10〜20行）
  → 変数名やリソース名は必要に応じて汎用化
-->

<!--
TODO【コード片】: Terraform破棄のトリガー部分
  → PRクローズ/マージ時にterraform destroyを走らせるActionsワークフロー抜粋
  → 「ちゃんと片付けもされるんだ」が伝わる
-->

<!--
TODO【コード片】: QRコード生成 + PRコメント投稿のスクリプト抜粋
  → プレビューURL取得 → QR生成 → PRにコメント投稿の流れ
-->

### 4-7. 定期スコアリング＆自動リファクタ

コードベースの健全性を維持するために、AIによる定期スコアリングの仕組みを導入している。

**スコアリング観点：**

1. アーキテクチャ
2. コード品質
3. テスト
4. セキュリティ
5. パフォーマンス・運用性

モノレポ内の各プロジェクトに対して、これら5つの観点でAIがスコアを算出する。スコアが低い箇所については、自動でIssueが起票され、リファクタ用のPRが生成される。

<!--
TODO【スクショ or テーブル】: 実際のスコアリング結果の例
  → 例:
    | プロジェクト         | アーキテクチャ | コード品質 | テスト | セキュリティ | パフォーマンス |
    |---------------------|-------------|----------|-------|------------|-------------|
    | customer-mobile     | 6.2         | 5.8      | 3.1   | 7.0        | 6.5         |
    | operator-frontend   | 7.5         | 7.2      | 4.0   | 7.8        | 7.0         |
    | backend             | 8.0         | 7.5      | 6.2   | 8.5        | 7.8         |
  → 実際の数字を入れる。テストが低い等のリアルさが説得力になる
-->

<!--
TODO【スクショ】: スコアリング結果から自動起票されたIssueの例
  → 「customer-mobile: テストカバレッジ改善」等のIssueタイトル
  → Issue本文にAIが書いた改善提案が載っている状態
-->

<!--
TODO【コード片】: スコアリングを実行するスクリプト or Actionsワークフロー抜粋
  → 各プロジェクトを走査 → Claude APIでスコア算出 → 結果をIssue化 の骨格
-->

現状はまだスコアリングに基づく優先度付けを精査している段階で、完全自動化には至っていない。今後はスコアリングのタイミングでIssueを自動起票し、そのIssueを起点にリファクタエージェントが自律的に修正PRを上げるフローを確立する。

---

## 5. まだできていないこと・これからやること

### 5-1. カスタムリントの作成

現在、AIエージェントはシステムプロンプト内で「この規約に従って実装すること」と指示している。しかしこれでは、エージェントがプロンプトの規約を読み解く処理に認知リソースを消費し、肝心のビジネスロジック実装に集中できない場面がある。

カスタムリントを整備すれば、コード規約の遵守はリンターに任せられる。エージェントの認知負荷を下げ、よりポン出しで正しいコードが書けるようにする。

<!--
TODO【コード片】: カスタムリントの具体的なルール定義 2〜3個
  → 例1: 「APIレスポンスは必ずこの型で返すこと」のルール
  → 例2: 「コンポーネントのファイル構成はこのパターンに従うこと」のルール
  → 各ルール5〜15行程度。「うちでもこういうルール作りたい」と思わせる具体度
-->

<!--
TODO【ポンチ絵】: カスタムリントがAIの認知負荷を下げる構造図
  → Before: エージェント → [プロンプトで規約確認 + ビジネスロジック実装]（負荷高い）
  → After: リンター → [規約チェック自動] + エージェント → [ビジネスロジックに集中]（負荷低い）
-->

### 5-2. E2Eテスト（Maestro + Playwright）

Maestro（モバイル）とPlaywright（Web）を組み合わせたE2Eテスト基盤を構築する。

目指しているのは、要件定義の段階で動作シナリオを自動生成し、実装完了後にそのシナリオでE2Eテストが自動実行される世界だ。ユーザー入力→管理画面でのオペレーター操作→結果確認までの一気通貫のテストを自動化する。

<!--
TODO【ポンチ絵】: E2Eテスト自動化の理想フロー図
  → 要件Issue → 動作シナリオ自動生成 → 実装完了 → Maestro/Playwrightでシナリオ実行 → 結果レポート
  → 「ユーザー操作 → オペレーター画面 → DB → レスポンス確認」の一気通貫が見える
-->

<!--
TODO【コード片】: MaestroのテストYAMLサンプル（あれば）
  → 実際に書き始めているテストシナリオの抜粋
  → まだ着手前なら省略可
-->

### 5-3. 本番バグ自動検知 → 自動修正

Sentryでフロントエンドのエラーを、バックエンドのログでサーバー側の異常を検知する。検知したバグに対して、AIエージェントが原因を特定し、修正PRを自動で上げる仕組みを作る。

将来的には、バグ検知→原因特定→修正PR作成→CIパス確認までが自律的に回るようにしたい。

<!--
TODO【ポンチ絵】: バグ自動修正の理想フロー図
  → Sentryアラート発火 / バックエンドログ異常検知
  → エージェントがスタックトレース・ログを解析
  → 原因箇所を特定
  → 修正PRを自動作成
  → CIパス確認
  → 人間がレビュー → マージ
  → 「夜中にバグ発生→朝には修正PRが上がっている」のストーリーが見える図
-->

### 5-4. ログ・メトリクス・BigQuery → UI/UX改善

バックエンドのメトリクス・ログ、DBの読み書きパフォーマンス、BigQueryに蓄積されたユーザー行動データ。これらを統合的に分析し、UI/UXの改善提案をAIエージェントに行わせることを計画している。

### 5-5. Claude Code → Figma → React Native パイプライン

デザインワークフローの自動化を目指している。

```
要求（こういうデザインにしたい）
  → Claude Codeがデザイントークンを読み込み、Figmaファイルを自動生成
  → 人間がFigma上でレビュー・修正・追加プロンプト投入
  → Reactコードを自動生成
  → React Nativeに合流
```

デザイントークンをベースにすることで、ブランド一貫性を保ちながらデザイン→実装のサイクルを高速化する。

<!--
TODO【ポンチ絵】: デザイン→実装パイプラインの全体図
  → 要求 → Claude Code（デザイントークン読み込み）→ Figmaファイル生成 → 人間レビュー/修正 → Reactコード生成 → React Nativeに合流
  → 各ステップで「自動」「人間」を色分け
-->

<!--
TODO【スクショ】: デザイントークンの定義ファイル抜粋（あれば）
  → カラーパレット、タイポグラフィ、スペーシング等の定義
  → 「このトークンを読み込んでAIがデザインを起こす」のイメージが湧くもの
-->

### 5-6. エージェント自己改善

過去に実装されたPRを分析し、繰り返し発生しているミスや非効率なパターンを検出する。根本原因がエージェントのプロンプトやワークフロー設計にある場合、エージェント自身がそれを修正する仕組みを構築したい。

また、日常的な会話やフィードバックのログをどのようにエージェントのコンテキストに反映させるか——意思決定レイヤーへの情報伝達の仕組みも今後の課題だ。

<!--
TODO【ポンチ絵】: エージェント自己改善のフィードバックループ図
  → 過去PR分析 → パターン検出 → プロンプト/ワークフロー修正 → 次回のPR品質向上
  → 日常会話ログ → コンテキスト注入 → エージェントの判断精度向上
-->

### 5-7. チャットツールの自前実装

<!-- TODO: 詳細があれば記載 -->

現在利用しているチャット機能を自前で書き直す計画がある。

---

## まとめ

この2ヶ月で構築した仕組みの核は、突き詰めると以下の3点に集約される。

**開発プロセスを細かく分解したこと。** AIに一気にやらせず、ステップごとに検証可能な粒度にした。

**すべてをGitHub上で可視化したこと。** Issue・PR・ラベル・Projectsで全タスクの状態が一目瞭然になり、人間同士のコミュニケーションコストが激減した。

**ラベル駆動でエージェントが自律的にバトンを渡す仕組みを作ったこと。** 人間はIssueを立てるだけ。あとはエージェントが回す。

まだまだ未完成な部分は多い。だが、この基盤が回り始めてからの開発速度は、以前とは比較にならない。本記事の設定やワークフローが、同じような課題を抱えるチームの参考になれば幸いだ。

<!-- TODO: 筆者プロフィール -->

---

<!--
====================================================
  TODO チェックリスト（記事完成前に全て埋める）
====================================================

■ スクショ系（撮影 or 既存から収集）
  □ GitHub Projectsボード俯瞰
  □ Issueツリー構造（親Issue + サブIssue + PR紐付き）
  □ Projectsボードのビフォーアフター3連（カード移動の流れ）
  □ 寝てたらマージレディになっていたPR（時系列が見えるもの）
  □ Issueタイムラインでラベルが自動遷移している様子
  □ エージェントが書いたPRのdiff画面
  □ GitHub Actions実行一覧（複数ワークフロー同時稼働）
  □ PRのChecksタブ（大量のCIチェックが並んでいる）
  □ 破壊的変更チェックがCIで止めた実例
  □ プレビュー環境4連（Actions発火→Terraform実行→QRコード投稿→スマホ確認）
  □ スコアリング結果から自動起票されたIssue
  □ 2ヶ月前のSlack/GitHub（ビフォー感、あれば）

■ コード片系（リポジトリから抜粋）
  □ lefthook.yml 抜粋
  □ サブIssue自動生成のActionsワークフロー
  □ ステータス自動遷移のActionsワークフロー
  □ ラベルトリガー→Claude API呼び出しのActionsワークフロー
  □ 次ラベル自動付与の処理
  □ 代表エージェントのシステムプロンプト抜粋
  □ Claude API呼び出しスクリプトの骨格
  □ 破壊的変更チェックのスクリプト/ワークフロー
  □ Terraform構築（プレビュー環境リソース定義）
  □ Terraform破棄トリガーのActionsワークフロー
  □ QRコード生成+PRコメント投稿スクリプト
  □ スコアリング実行スクリプト/ワークフロー
  □ カスタムリントのルール定義 2〜3個

■ ポンチ絵系（作成）
  □ チーム構成図（人間 + AIエージェント30体の役割分担）
  □ 全体アーキテクチャ図（GitHub Actions中心の放射構成）
  □ 開発プロセスのステップ図（要求→devマージ、担当者明記）
  □ ラベル駆動フローの全体図（バトンリレー + 人間介入ポイント）
  □ カスタムリントによるAI認知負荷軽減の構造図
  □ E2Eテスト自動化の理想フロー図
  □ バグ自動修正の理想フロー図
  □ デザイン→実装パイプライン全体図
  □ エージェント自己改善フィードバックループ図

■ 数字系（集計）
  □ エージェント正確な数（現時点の全人格数）
  □ 導入前後の定量比較（PR数、消化速度、コミュニケーション量）
  □ CIチェック数のビフォーアフター
  □ スコアリング結果の実数値

■ データ系（Claude Codeで生成）
  □ 全エージェント一覧 + 各4行の役割説明

■ その他
  □ サービス名・プロダクト概要
  □ 筆者プロフィール
  □ 具体例パターン: 新機能追加の1件通しの実例（時系列付き）
  □ 具体例パターン: バグ修正の1件通しの実例
  □ デザイントークン定義ファイル抜粋
  □ チャットツール自前実装の詳細（あれば）
-->
